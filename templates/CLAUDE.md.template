# {{プロジェクト名}}

このファイルは Claude Code（KKS-Forge）に読ませる、このプロジェクト専用の前提
情報です。要件定義書 v3.0 §4.2.2 / §9 Phase 4 に基づき配置しています。

## 1. 何をするプロジェクトか

- 一行説明: {{ここに 1 行で書く}}
- 主要技術: {{言語 / FW / DB}}
- 本番 URL: {{あれば}}

## 2. ディレクトリ構造

```
{{tree -L 2 の出力など}}
```

- `src/`     … {{役割}}
- `tests/`   … {{役割}}
- `scripts/` … {{役割}}

## 3. 触ってよい・触ってはいけない箇所

- ✅ 自由に変更してよい: `src/`, `tests/`, `docs/`
- ⚠️ 変更前に必ず人間に確認: `migrations/`, `.env*`, `infra/`
- 🚫 触らない: `vendor/`, `dist/`, `node_modules/`, 本番 DB に直結する SQL

## 4. 動作確認の手順

```bash
# 開発サーバ起動
{{コマンド}}

# テスト
{{コマンド}}

# Lint / 型チェック
{{コマンド}}
```

## 5. デプロイ

- 手段: {{GitHub Actions / 手動 ssh など}}
- 注意: {{ロールバック手段}}

## 6. 既知の罠 / 過去にハマったこと

- {{箇条書き}}

## 7. 関連リポジトリ・サービス

- {{依存している外部サービス}}

---

> KKS-Forge から開く際は、まずこのファイルを読んでから作業を始めてください。
